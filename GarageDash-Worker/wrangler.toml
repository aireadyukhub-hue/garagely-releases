name = "garagely-backend"
main = "src/index.ts"
compatibility_date = "2025-05-01"
compatibility_flags = ["nodejs_compat"]

# Non-secret config lives here (committed plaintext — safe).
# Secrets are set with `wrangler secret put NAME` (see DEPLOY.md):
#   SUPABASE_SERVICE_ROLE_KEY, STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET,
#   ADMIN_SECRET, RESEND_API_KEY (optional — licence/reset emails)
# Cloudflare Workers AI — powers the free in-app help assistant.
[ai]
binding = "AI"

[vars]
SUPABASE_URL = "https://ulrsthmkgsyfeloihens.supabase.co"
TRIAL_DAYS = "14"
STRIPE_PRICE_ID = "price_1TjmjIHuEMj7eABe0lLdMQfN"   # the live subscription price id
SITE_URL = "https://garagedash.co.uk"           # marketing site URL (Stripe redirects)
RESEND_FROM = "GarageDash <info@garagedash.co.uk>"  # verified sender — garagedash.co.uk verified in Resend 6 Jul 2026
