# GarageDash desktop renderer — Supabase connection (Vite reads VITE_* at build time)
VITE_SUPABASE_URL=https://ulrsthmkgsyfeloihens.supabase.co
# Supabase → Settings → API Keys → Legacy → anon public key. Safe in the client (RLS protects data).
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here
